# Environment Variables

- Never invent env var names. Search `.env*`, README, and code usage first.
- If a new env var is required, propose the name + where it should be documented and validated at startup.
