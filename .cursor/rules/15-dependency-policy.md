# Dependency Policy

- Do not add new dependencies without explicit approval.
- If a new dependency is truly needed, propose:
  - why it is required
  - why existing deps cannot solve it
  - alternative approach without new deps
